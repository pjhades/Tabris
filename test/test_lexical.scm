(define (fact n)
  (if (< n 2)
	1
	(* n (fact (- n 1)))))

(define (foo x); first "line"
  (list x
		'(+ 5 'x)
		"this is not ' quote" ;)
		)); end!





       (foo ; now let's take some roooooooock music!
	   "this is a string"
	   '          

''''    ''(


	   this is the symbol quasiquote)

	   )


	   '()


(incomplete 'ok)






